name: ShatterBreak
options:
  bundleIdPrefix: la
  deploymentTarget:
    macOS: "15.0"
  developmentLanguage: en
  # Keep generated project artifacts inside the project directory
  # instead of the default ~/Library/Developer/Xcode/DerivedData
  xcodeVersion: "16.3"

settings:
  base:
    # --- Deployment ---
    MACOSX_DEPLOYMENT_TARGET: "15.0"
    SUPPORTED_PLATFORMS: macosx
    SUPPORTS_MACCATALYST: NO
    SDKROOT: auto

    # --- Team & Signing ---
    DEVELOPMENT_TEAM: XSDWUU9ZQ2

    # --- Build output (project-local) ---
    SYMROOT: $(SRCROOT)/build
    OBJROOT: $(SRCROOT)/build/Intermediates.noindex

    # --- Swift ---
    SWIFT_VERSION: "5.0"
    SWIFT_APPROACHABLE_CONCURRENCY: YES
    SWIFT_DEFAULT_ACTOR_ISOLATION: MainActor
    SWIFT_UPCOMING_FEATURE_MEMBER_IMPORT_VISIBILITY: YES
    SWIFT_EMIT_LOC_STRINGS: YES

    # --- Compiler warnings (mirrors existing pbxproj) ---
    CLANG_ANALYZER_NONNULL: YES
    CLANG_ANALYZER_NUMBER_OBJECT_CONVERSION: YES_AGGRESSIVE
    CLANG_WARN_BLOCK_CAPTURE_AUTORELEASING: YES
    CLANG_WARN_BOOL_CONVERSION: YES
    CLANG_WARN_COMMA: YES
    CLANG_WARN_CONSTANT_CONVERSION: YES
    CLANG_WARN_DEPRECATED_OBJC_IMPLEMENTATIONS: YES
    CLANG_WARN_DIRECT_OBJC_ISA_USAGE: YES_ERROR
    CLANG_WARN_DOCUMENTATION_COMMENTS: YES
    CLANG_WARN_EMPTY_BODY: YES
    CLANG_WARN_ENUM_CONVERSION: YES
    CLANG_WARN_INFINITE_RECURSION: YES
    CLANG_WARN_INT_CONVERSION: YES
    CLANG_WARN_NON_LITERAL_NULL_CONVERSION: YES
    CLANG_WARN_OBJC_IMPLICIT_RETAIN_SELF: YES
    CLANG_WARN_OBJC_LITERAL_CONVERSION: YES
    CLANG_WARN_OBJC_ROOT_CLASS: YES_ERROR
    CLANG_WARN_QUOTED_INCLUDE_IN_FRAMEWORK_HEADER: YES
    CLANG_WARN_RANGE_LOOP_ANALYSIS: YES
    CLANG_WARN_SUSPICIOUS_MOVE: YES
    CLANG_WARN_UNGUARDED_AVAILABILITY: YES_AGGRESSIVE
    CLANG_WARN_UNREACHABLE_CODE: YES
    CLANG_WARN__DUPLICATE_METHOD_MATCH: YES
    GCC_WARN_64_TO_32_BIT_CONVERSION: YES
    GCC_WARN_ABOUT_RETURN_TYPE: YES_ERROR
    GCC_WARN_UNDECLARED_SELECTOR: YES
    GCC_WARN_UNINITIALIZED_AUTOS: YES_AGGRESSIVE
    GCC_WARN_UNUSED_FUNCTION: YES
    GCC_WARN_UNUSED_VARIABLE: YES
    GCC_NO_COMMON_BLOCKS: YES
    DEAD_CODE_STRIPPING: YES
    ENABLE_STRICT_OBJC_MSGSEND: YES
    ENABLE_USER_SCRIPT_SANDBOXING: YES

    # --- Asset catalog ---
    ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS: YES
    STRING_CATALOG_GENERATE_SYMBOLS: YES
    LOCALIZATION_PREFERS_STRING_CATALOGS: YES

  configs:
    Debug:
      SWIFT_ACTIVE_COMPILATION_CONDITIONS: DEBUG
      SWIFT_OPTIMIZATION_LEVEL: "-Onone"
      DEBUG_INFORMATION_FORMAT: dwarf
      GCC_OPTIMIZATION_LEVEL: "0"
      GCC_PREPROCESSOR_DEFINITIONS:
        - "DEBUG=1"
        - "$(inherited)"
      MTL_ENABLE_DEBUG_INFO: INCLUDE_SOURCE
      MTL_FAST_MATH: YES
      ONLY_ACTIVE_ARCH: YES
      ENABLE_TESTABILITY: YES

    Release:
      SWIFT_COMPILATION_MODE: wholemodule
      DEBUG_INFORMATION_FORMAT: dwarf-with-dsym
      ENABLE_NS_ASSERTIONS: NO
      MTL_ENABLE_DEBUG_INFO: NO
      MTL_FAST_MATH: YES
      COPY_PHASE_STRIP: NO

targets:
  ShatterBreak:
    type: application
    platform: macOS
    deploymentTarget: "15.0"

    sources:
      # XcodeGen recursively picks up all .swift files and resources
      - path: ShatterBreak
        excludes:
          - "**/*.md"   # requirements.md stays in source but not compiled

    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: la.ShatterBreak
        PRODUCT_NAME: $(TARGET_NAME)
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"

        # --- Code signing ---
        CODE_SIGN_STYLE: Automatic
        "CODE_SIGN_IDENTITY[sdk=macosx*]": Apple Development

        # --- Hardened Runtime & Sandbox ---
        ENABLE_HARDENED_RUNTIME: YES
        ENABLE_APP_SANDBOX: YES

        # --- Entitlements ---
        CODE_SIGN_ENTITLEMENTS: ShatterBreak/ShatterBreak.entitlements

        # --- Info.plist (generated) ---
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: ShatterBreak
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.productivity
        INFOPLIST_KEY_LSUIElement: YES

        # --- Linker ---
        LD_RUNPATH_SEARCH_PATHS: "@executable_path/../Frameworks"

        # --- Previews ---
        ENABLE_PREVIEWS: YES

    scheme:
      # Shared scheme generated automatically by XcodeGen
      buildAction:
        parallelizeBuild: true
        buildImplicitDependencies: true
      testAction:
        config: Debug
      launchAction:
        config: Debug
      profileAction:
        config: Release
      analyzeAction:
        config: Debug
      archiveAction:
        config: Release
